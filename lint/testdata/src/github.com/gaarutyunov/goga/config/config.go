// Package config is a clean fixture: a top-level goga module, which is exactly
// what the flat layout asks for. It must produce no diagnostic.
package config

// Helper exists so the fixture is a package with content.
func Helper() string { return "config" }
